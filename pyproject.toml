[project]
name = "mlflow-skills-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=3.12",
    "litellm",
    "python-dotenv",
    "pyyaml",
]

[tool.pyright]
include = ["tests"]
venvPath = "."
venv = ".venv"
